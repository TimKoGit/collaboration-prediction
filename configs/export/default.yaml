# Model export configuration (ONNX, etc.)

auto_export_onnx: false

onnx:
  export_path: ./models/model.onnx
  opset_version: 14
  verification:
    rtol: 1e-2
    atol: 5e-2

inference:
  triton:
    model_repository: ./models/triton_models
